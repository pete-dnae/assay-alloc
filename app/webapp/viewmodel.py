class ViewModel(object):
    """
    The ViewModel class encapsulates all the values that are required to
    parameterise the web view's html rendering. Used as the data model object
    in conjunction with Flask's rendering templates.
    """

    def __init__(self):
        pass
