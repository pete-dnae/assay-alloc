# An aide-memoir

Make sure running this in target OS !!! - probably Windows.

cd c:\docker-share\assay-alloc\app\cmdlineapp>

pyinstaller --onefile -p dir;dir;dir assay-alloc.py

The executable lives in app\dist
